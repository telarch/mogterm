{
  "name": "mogterm",
  "version": "0.1.0",
  "description": "MogTerm terminal emulator engine with automated test suite",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
